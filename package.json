{
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  },
  "scripts": {
    "dev": "dotnet fable watch src --outDir build --run vite",
    "build": "tsc --noEmit && dotnet fable src --outDir build && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  }
}
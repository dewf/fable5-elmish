{
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  },
  "scripts": {
    "dev": "dotnet fable watch src --outDir build --run vite",
    "build": "dotnet fable src --outDir build --run vite build",
    "preview": "vite preview"
  }
}
